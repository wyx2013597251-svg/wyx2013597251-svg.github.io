<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>雨轩餐厅</title>

<style>
body {
  margin: 0;
  font-family: "Arial";
  background: #fafafa;
}

header {
  background: url("https://picsum.photos/1200/500?food") center/cover;
  color: white;
  text-align: center;
  padding: 100px 20px;
}

header h1 {
  font-size: 50px;
}

.container {
  padding: 20px;
}

h2 {
  border-left: 5px solid #ff7043;
  padding-left: 10px;
}

.gallery {
  display: flex;
  flex-wrap: wrap;
}

.gallery img {
  width: 300px;
  margin: 10px;
  border-radius: 10px;
}

.menu {
  background: white;
  padding: 15px;
  border-radius: 10px;
}

.menu-item {
  display: flex;
  justify-content: space-between;
  margin: 10px 0;
}

input, textarea {
  width: 100%;
  padding: 10px;
  margin-top: 10px;
}

button {
  background: #ff7043;
  color: white;
  border: none;
  padding: 10px;
  margin-top: 10px;
  cursor: pointer;
}

.message {
  background: white;
  padding: 10px;
  margin-top: 10px;
  border-radius: 8px;
}

iframe {
  width: 100%;
  height: 300px;
  border: none;
  border-radius: 10px;
}
</style>

</head>

<body>

<header>
  <h1>🍽️ 雨轩餐厅</h1>
  <p>美味 · 温馨 · 享受生活</p>
</header>

<div class="container">

<h2>📸 餐厅展示</h2>
<div class="gallery">
  <img src="https://picsum.photos/300/200?food1">
  <img src="https://picsum.photos/300/200?food2">
  <img src="https://picsum.photos/300/200?food3">
</div>

<h2>📋 菜单</h2>
<div class="menu">
  <div class="menu-item"><span>招牌牛排</span><span>¥88</span></div>
  <div class="menu-item"><span>意大利面</span><span>¥58</span></div>
  <div class="menu-item"><span>炸鸡拼盘</span><span>¥45</span></div>
  <div class="menu-item"><span>奶茶</span><span>¥15</span></div>
</div>

<h2>🗺️ 地址</h2>
<iframe src="https://maps.google.com/maps?q=Tokyo&t=&z=13&ie=UTF8&iwloc=&output=embed"></iframe>

<h2>💬 留言板</h2>

<input type="text" id="name" placeholder="你的名字">
<textarea id="message" placeholder="说点什么..."></textarea>
<button onclick="addMessage()">提交留言</button>

<h3>留言数量：<span id="count">0</span></h3>

<div id="messages"></div>

</div>

<script>

// 从本地读取
let messages = JSON.parse(localStorage.getItem("messages")) || [];

displayMessages();

function addMessage() {
  let name = document.getElementById("name").value;
  let text = document.getElementById("message").value;

  if(name === "" || text === "") {
    alert("请填写完整！");
    return;
  }

  messages.unshift({name, text});
  localStorage.setItem("messages", JSON.stringify(messages));

  displayMessages();

  document.getElementById("name").value = "";
  document.getElementById("message").value = "";
}

function displayMessages() {
  let container = document.getElementById("messages");
  container.innerHTML = "";

  messages.forEach(msg => {
    let div = document.createElement("div");
    div.className = "message";
    div.innerHTML = "<b>" + msg.name + "：</b>" + msg.text;
    container.appendChild(div);
  });

  document.getElementById("count").innerText = messages.length;
}

</script>

</body>
</html>