<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>背叛</title>

<style>
body {
    margin: 0;
    background: #000;
    color: #fff;
    font-family: Arial;
    text-align: center;
}

/* 粒子背景 */
#bg {
    position: fixed;
    top: 0;
    left: 0;
    z-index: -1;
}

/* 头像 */
.avatar {
    width: 160px;
    height: 160px;
    border-radius: 50%;
    border: 4px solid #ff2d75;
    box-shadow: 0 0 30px #ff2d75;
    margin-top: 40px;
}

/* 名字 */
.name {
    font-size: 32px;
    margin: 15px 0;
    text-shadow: 0 0 10px #ff2d75;
}

/* 文案 */
.text-box {
    margin: 20px;
    padding: 20px;
    background: rgba(255,0,80,0.1);
    border-radius: 15px;
    box-shadow: 0 0 30px #ff2d75;
    line-height: 1.8;
    text-align: left;
}

/* 光标 */
.cursor {
    display: inline-block;
    width: 10px;
    background: #ff2d75;
    animation: blink 1s infinite;
}

@keyframes blink {
    0%,50%,100% {opacity:1;}
    25%,75% {opacity:0;}
}

/* TG */
.tg {
    margin: 20px;
}

/* 按钮 */
.btn {
    display: inline-block;
    padding: 12px 25px;
    margin: 10px;
    background: linear-gradient(45deg,#ff2d75,#ff6a00);
    border-radius: 30px;
    box-shadow: 0 0 15px #ff2d75;
    cursor: pointer;
}

/* 底图 */
.bottom-img {
    width: 90%;
    border-radius: 15px;
    margin: 20px auto;
}
</style>
</head>

<body>

<canvas id="bg"></canvas>

<!-- 音频 -->
<audio id="music" src="music.mp3" loop></audio>
<audio id="typeSound" src="type.mp3"></audio>

<img src="avatar.jpg" class="avatar">
<div class="name">背叛</div>

<div class="btn" onclick="toggleMusic()">🎵 播放 / 暂停</div>

<div class="text-box">
<span id="text"></span><span class="cursor"></span>
</div>

<div class="tg">
本人TG✈️：@beipancnm<br><br>
需要任何业务联系：开户 远控 点号技术 出app账号 三色人脸 代付 银行卡 手机卡 查档 定位 办证 京东E卡 手机话费 卡扣 代还网贷 账号解封 代开飞机会员长期合作互赢
</div>

<img src="bottom.jpg" class="bottom-img">

<script>
// 🎵 音乐控制
const music = document.getElementById("music");

function toggleMusic() {
    if (music.paused) {
        music.play();
    } else {
        music.pause();
    }
}

// 手机自动播放
document.addEventListener("click", () => {
    music.play().catch(()=>{});
});


// ⌨️ 打字机 + 声音
const content = `希望看到这里的宝子们 祝你们平安顺遂 越来越好 万事如意 无忧无虑 平安喜乐 岁岁常欢愉 万事皆胜意 一切尽意 百事从欢 前途似锦 天天开心 无忧无虑  平安顺遂  好好睡觉 许你自由 愿你无忧 平安喜樂 年年无碍 岁岁无忧 四季冗长 万事顺意 岁岁常欢愉 万事皆胜意知命不惧 日日自新 岁岁无忧 一切尽意 百事从欢 幸福快乐家庭和睦 天天向上 得偿所愿一帆风顺，二龙飞腾，三阳开泰，四季平安，五福临门，六六大顺，七星高照，八方来财，九九同心，十全十美，福星高照，洪福齐天，长命富贵，麒麟送福，旭日东升，鱼跃龙门，杞菊延年，寿比南山，福如东海，万代长春，喜如天降，平安如意，天降鸿福，年年有余，万事顺利`;

let i = 0;
const typeSound = document.getElementById("typeSound");

function typeWriter() {
    if (i < content.length) {
        document.getElementById("text").innerHTML += content.charAt(i);

        // 打字声音
        if (typeSound) {
            typeSound.currentTime = 0;
            typeSound.play().catch(()=>{});
        }

        i++;
        setTimeout(typeWriter, 40);
    }
}

typeWriter();


// 💥 点击爆炸特效
document.addEventListener("click", function(e){
    for(let i=0;i<10;i++){
        let dot=document.createElement("div");
        dot.style.position="fixed";
        dot.style.left=e.clientX+"px";
        dot.style.top=e.clientY+"px";
        dot.style.width="5px";
        dot.style.height="5px";
        dot.style.background="#ff2d75";
        dot.style.borderRadius="50%";
        document.body.appendChild(dot);

        let x=(Math.random()-0.5)*100;
        let y=(Math.random()-0.5)*100;

        dot.animate([
            {transform:"translate(0,0)",opacity:1},
            {transform:`translate(${x}px,${y}px)`,opacity:0}
        ],{duration:600});

        setTimeout(()=>dot.remove(),600);
    }
});


// 🌌 粒子背景
const canvas = document.getElementById("bg");
const ctx = canvas.getContext("2d");

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

let particles = [];

for (let i = 0; i < 80; i++) {
    particles.push({
        x: Math.random()*canvas.width,
        y: Math.random()*canvas.height,
        vx: Math.random()-0.5,
        vy: Math.random()-0.5
    });
}

function draw(){
    ctx.clearRect(0,0,canvas.width,canvas.height);

    particles.forEach(p=>{
        p.x+=p.vx;
        p.y+=p.vy;

        if(p.x<0||p.x>canvas.width)p.vx*=-1;
        if(p.y<0||p.y>canvas.height)p.vy*=-1;

        ctx.beginPath();
        ctx.arc(p.x,p.y,2,0,Math.PI*2);
        ctx.fillStyle="#fff";
        ctx.fill();
    });

    for(let i=0;i<particles.length;i++){
        for(let j=i+1;j<particles.length;j++){
            let dx=particles[i].x-particles[j].x;
            let dy=particles[i].y-particles[j].y;
            let dist=Math.sqrt(dx*dx+dy*dy);

            if(dist<120){
                ctx.beginPath();
                ctx.moveTo(particles[i].x,particles[i].y);
                ctx.lineTo(particles[j].x,particles[j].y);
                ctx.strokeStyle="rgba(255,255,255,0.2)";
                ctx.stroke();
            }
        }
    }

    requestAnimationFrame(draw);
}

draw();
</script>

</body>
</html>